{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for phonebook",
  "license": "ISC",
  "author": "James Broadbent",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd C:/Workspace/fullStack-open/part3/phonebook && npm run build && cp -r dist C:/Workspace/fullStack-open/part3/phonebook/backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
